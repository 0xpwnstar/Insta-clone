version: '2'

services:
  insta:
    build:
      context: .
      dockerfile: DOCKERFILE
    container_name: insta
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - node_modules:/usr/src/app/node_modules